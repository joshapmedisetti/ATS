server:
  port: 8080

spring:
  application:
    name: ATS

  datasource:
    url: jdbc:mysql://localhost:3306/ats
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: none

    show-sql: true

    properties:
      hibernate:
        format_sql: true